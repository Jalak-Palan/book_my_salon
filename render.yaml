services:
  - type: web
    name: stockify-backend
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://jalakapalan:jalak123@cluster0.dqhwnjd.mongodb.net/stock-management
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 5000
